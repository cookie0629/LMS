# LMS 项目环境配置
# 使用 direnv 或手动 source 此文件

# Wt pkg-config 路径
# pkg-config 是一个帮助查找库文件编译和链接参数的工具
# PKG_CONFIG_PATH 是 pkg-config 搜索 .pc 文件的目录路径
# 这行代码：
# 将 /usr/local/lib/pkgconfig 添加到搜索路径的最前面
# 保留原有的 PKG_CONFIG_PATH 内容（${PKG_CONFIG_PATH}）
# 使用冒号 : 分隔多个路径（Linux/Unix 标准）
# 作用：让编译器能找到 Wt 库的编译信息
export PKG_CONFIG_PATH="/usr/local/lib/pkgconfig:${PKG_CONFIG_PATH}"


# 库路径
# LD_LIBRARY_PATH 是系统搜索动态库（.so 文件）的路径
# 这行代码：
# 将 /usr/local/lib 添加到动态库搜索路径的最前面
# 保留原有的 LD_LIBRARY_PATH 内容
# 作用：让程序运行时能找到 Wt 库文件
export LD_LIBRARY_PATH="/usr/local/lib:${LD_LIBRARY_PATH}"

# 显示配置
echo "PKG_CONFIG_PATH: ${PKG_CONFIG_PATH}"
echo "LD_LIBRARY_PATH: ${LD_LIBRARY_PATH}"
