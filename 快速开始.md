# LMS 项目快速开始

## 🚀 三步开始

### 步骤 1: 进入 WSL2 环境

在 PowerShell 中运行：
```powershell
wsl
```

### 步骤 2: 进入项目目录

在 WSL 中运行：
```bash
# 如果项目在 Windows 文件系统中
cd /mnt/e/LightweightMusicServer

# 或者如果项目在 WSL 文件系统中
cd ~/LightweightMusicServer
```

### 步骤 3: 运行一键安装脚本

```bash
bash scripts/setup-environment.sh
```

这个脚本会自动：
- ✅ 更新系统包
- ✅ 安装所有开发工具
- ✅ 安装所有依赖库
- ✅ 编译安装 Wt 框架
- ✅ 验证安装

**预计时间：20-40 分钟**（取决于网络和编译速度）

---

## 📋 手动安装（如果自动脚本失败）

如果自动脚本遇到问题，可以按照以下步骤手动安装：

### 1. 更新系统
```bash
sudo apt update && sudo apt upgrade -y
```

### 2. 安装基础工具
```bash
sudo apt install -y build-essential cmake git pkg-config g++-12
```

### 3. 安装依赖库
```bash
sudo apt install -y \
  libboost-all-dev \
  libavcodec-dev libavformat-dev libavutil-dev \
  libtag1-dev libssl-dev libsqlite3-dev \
  libpugixml-dev libconfig++-dev
```

### 4. 安装 Wt 框架
```bash
bash scripts/install-wt.sh
```

### 5. 验证安装
```bash
bash scripts/check-dependencies.sh
```

---

## 🔍 验证安装

运行依赖检查脚本：
```bash
bash scripts/check-dependencies.sh
```

应该看到所有依赖都显示 ✅

---

## 🏗️ 构建项目

安装完成后，开始构建项目：

```bash
# 1. 创建构建目录
mkdir -p build
cd build

# 2. 配置 CMake
cmake .. -DCMAKE_BUILD_TYPE=Debug

# 3. 编译
make -j$(nproc)

# 4. 检查结果
ls -lh src/lms/lms
```

---

## ❓ 遇到问题？

查看详细文档：
- **环境搭建指南.md** - 完整的环境搭建说明
- **详细开发步骤指南.md** - 详细的开发流程
- **项目学习指南.md** - 项目架构和学习路径

常见问题：
- 如果 Wt 安装失败，检查网络连接和磁盘空间
- 如果编译失败，检查编译器版本（需要 GCC 11+）
- 如果找不到库，运行 `sudo ldconfig`

---

## 📚 下一步

环境搭建完成后：
1. 阅读 `项目学习指南.md` 了解项目架构
2. 从 `src/lms/main.cpp` 开始阅读代码
3. 参考 `学习路径总结.md` 制定学习计划

祝你学习顺利！🎵

