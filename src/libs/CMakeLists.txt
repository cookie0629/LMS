# 核心库
# 这里将添加各个库的 CMakeLists.txt

# 示例：添加 core 库（当创建后）
# add_subdirectory(core)

# 示例：添加 audio 库（当创建后）
# add_subdirectory(audio)

# 示例：添加 database 库（当创建后）
# add_subdirectory(database)

message(STATUS "LMS 库目录 - 待实现各个库")

